services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: scribe
    ports: ["5433:5432"]
    volumes: [ "pg-data:/var/lib/postgresql/data" ]
volumes:
  pg-data: {}
